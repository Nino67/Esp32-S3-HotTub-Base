

#include "system_status.h"  







// esp_err_t system_status_init(SystemStatus_t *status)
// {
//     if (status == NULL) {
//         return ESP_ERR_INVALID_ARG;
//     }

//     status->uptime_us = 0;
//     status->core_0_free_heap = (float)heap_caps_get_free_size(MALLOC_CAP_INTERNAL | MALLOC_CAP_8BIT);
//     status->core_1_free_heap = (float)heap_caps_get_free_size(MALLOC_CAP_INTERNAL | MALLOC_CAP_8BIT);

//     return ESP_OK;
// }

